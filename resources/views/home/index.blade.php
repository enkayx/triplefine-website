<!DOCTYPE html>
<html>

@include('home.css')

<body>
    <!-- header section -->
    @include('home.header')

    <!-- slider section -->
      @include('home.slider')

  <!-- shop section -->
   @include('home.products')

  <!-- contact section -->
    @include('home.contact')


  <br><br><br>
   
  <!-- info section -->
  @include('home.footer')
</body>

</html>